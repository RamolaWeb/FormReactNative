import {
    StyleSheet,
  } from 'react-native'
  
const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  bodyContainer: {
    borderWidth: 1,
    borderColor: 'black',
    margin: 5,
    padding: 5,
  },
})

export default styles