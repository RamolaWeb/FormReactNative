import {
    StyleSheet,
  } from 'react-native'
  
const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  bodyContainer: {
    borderWidth: 1,
    borderColor: 'black',
    padding: 10,
  },
})

export default styles